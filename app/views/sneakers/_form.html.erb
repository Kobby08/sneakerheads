<%= simple_form_for(@sneaker, html: { data: { turbo: false} }) do |f| %>
  <%= f.input :name %>
  <%= f.input :model %>
  <%= f.input :description %>
  <%= f.input :price, input_html: {min: 0} %>
  <%= f.button :submit %>
<% end %>
